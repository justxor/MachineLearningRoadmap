# 📝 Задача 09 — NLP: Обработка Текста

**Сложность:** ⭐⭐⭐ | **Время:** 120–150 минут
**Разбор:** [solution-09-nlp.md](../practice-solutions/solution-09-nlp.md)

---

## 📋 Контекст

Стартап получает тысячи отзывов на свой продукт. Нужно автоматически определять тональность (positive / negative / neutral) и выделять ключевые темы для продуктовой команды.

---

## 📦 Датасет

```python
import pandas as pd
import numpy as np

reviews = [
    "Отличный продукт, очень доволен покупкой! Рекомендую всем",
    "Ужасное качество, сломалось через неделю. Деньги на ветер",
    "Нормально, ничего особенного. Работает как заявлено",
    "Супер! Лучший продукт что я покупал за последние годы",
    "Очень разочарован. Не соответствует описанию вообще",
    "Хорошее соотношение цена/качество для своей категории",
    "Прислали бракованный товар, поддержка не отвечает",
    "Всё чётко, доставка быстрая, товар соответствует фото",
    "Среднее качество, есть лучше за те же деньги на рынке",
    "Полный хлам! Выбросил сразу. Требую возврат средств!!!",
] * 200

np.random.seed(42)
sentiments = np.random.choice(['positive', 'negative', 'neutral'], len(reviews), p=[0.5, 0.3, 0.2])

df = pd.DataFrame({
    'review':    reviews,
    'sentiment': sentiments,
    'rating':    np.random.choice([1, 2, 3, 4, 5], len(reviews)),
    'date':      pd.date_range('2023-01-01', periods=len(reviews), freq='6h'),
})
```

---

## ✅ Задания

### Часть 1 — Предобработка текста

**1.1.** Реализуй функцию clean_text(text) которая:
- Приводит к нижнему регистру
- Удаляет знаки препинания, цифры, специальные символы
- Удаляет стоп-слова (русские: nltk)
- Токенизирует текст
Применяй к колонке review.

**1.2.** Добавь текстовые метапризнаки:
- review_length (символов)
- word_count (слов)
- avg_word_length
- exclamation_count (количество !)
- question_count (количество ?)
- uppercase_ratio (доля заглавных букв до lower)

**1.3.** Найди топ-20 самых частых слов в positive отзывах и топ-20 в negative. Есть ли пересечение? Построй wordcloud или bar chart.

---

### Часть 2 — Векторизация

**2.1.** Создай три варианта векторизации:
- CountVectorizer (bag of words)
- TfidfVectorizer (max_features=1000, ngram_range=(1,2))
- TfidfVectorizer с char ngrams (analyzer='char_wb', ngram_range=(3,5))

**2.2.** Примени SVD (n_components=50) к TF-IDF матрице. Сколько дисперсии объясняет?

**2.3.** Если есть доступ в интернет — загрузи предобученные векторы fasttext для русского языка и создай усреднённые эмбеддинги для каждого отзыва.

---

### Часть 3 — Классификация тональности

**3.1.** Раздели на train/test (80/20, стратификация). Обучи и сравни:
- Naive Bayes (MultinomialNB)
- Logistic Regression
- LinearSVC

Используй три варианта векторизации из части 2. Итого 9 комбинаций. Отчёт: Accuracy, F1-macro, F1-weighted.

**3.2.** Для лучшей комбинации выведи:
- Classification report (precision/recall/f1 по каждому классу)
- Confusion matrix
- Топ-10 слов, наиболее влияющих на класс "negative" (из LR коэффициентов)

**3.3.** Найди примеры где модель ошибается. Объясни возможную причину каждой ошибки.

---

### Часть 4 — Тематическое моделирование (бонус ⭐⭐)

**4.1.** Примени LDA к корпусу отзывов (n_topics=5). Выведи топ-10 слов для каждой темы. Дай темам смысловые названия (например: "Доставка", "Качество товара", "Поддержка").

**4.2.** Для каждого отзыва определи доминирующую тему. Как распределяется тональность внутри каждой темы? Построй heatmap: тема vs тональность.

**4.3.** Попробуй BERT для классификации (если есть GPU или хотя бы CPU и время):
```python
from transformers import pipeline
classifier = pipeline("text-classification", model="blanchefort/rubert-base-cased-sentiment")
```
Сравни с TF-IDF + LR. Какой подход лучше?

---

## 📝 Требования к ответу

- Сравнительная таблица всех 9 комбинаций (vectorizer × model)
- Анализ ошибок с конкретными примерами
- Топ-слова для positive и negative классов
- Вывод: какой подход лучше всего подходит для этой задачи и почему
