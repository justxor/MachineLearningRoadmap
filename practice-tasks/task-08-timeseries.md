# ⏱️ Задача 08 — Временные Ряды

**Сложность:** ⭐⭐⭐⭐ | **Время:** 150–180 минут
**Разбор:** [solution-08-timeseries.md](../practice-solutions/solution-08-timeseries.md)

---

## 📋 Контекст

Ты ML-инженер в ритейл-компании. Нужно предсказать ежедневные продажи на 30 дней вперёд для каждого магазина. Точность критична: недопоставка = потеря выручки, избыток = издержки на хранение.

---

## 📦 Датасет

```python
import pandas as pd
import numpy as np

np.random.seed(42)
dates = pd.date_range('2021-01-01', '2023-12-31', freq='D')
n = len(dates)

trend = np.linspace(100, 180, n)
seasonal_week = 20 * np.sin(2 * np.pi * np.arange(n) / 7)
seasonal_year = 30 * np.sin(2 * np.pi * np.arange(n) / 365.25)
noise = np.random.normal(0, 10, n)

sales = trend + seasonal_week + seasonal_year + noise
sales = np.maximum(sales, 0)

df = pd.DataFrame({
    'date':    dates,
    'sales':   sales.round(1),
    'is_holiday': (np.random.random(n) < 0.05).astype(int),
    'promo':      (np.random.random(n) < 0.1).astype(int),
    'temperature': np.random.normal(10, 15, n),
})
```

---

## ✅ Задания

### Часть 1 — Анализ временного ряда

**1.1.** Построй временной ряд sales. Визуально определи:
- Есть ли тренд?
- Есть ли сезонность? Какого периода?
- Есть ли аномалии (резкие скачки)?

**1.2.** Проведи тест Дики-Фуллера. Стационарный ли ряд?
Если нет — продифференцируй (diff(1)) и проверь снова.

**1.3.** Сделай декомпозицию ряда (seasonal_decompose, period=7). Выведи компоненты: trend, seasonal, residual. Какой % дисперсии объясняет каждая компонента?

**1.4.** Построй ACF и PACF. Какие лаги значимы? Что это говорит о структуре ряда?

---

### Часть 2 — Создание признаков

**2.1.** Создай все временные признаки из date:
- year, month, day, dayofweek, dayofyear, weekofyear, quarter
- is_weekend, is_month_start, is_month_end
- Циклические: month_sin/cos, dow_sin/cos

**2.2.** Создай лаговые признаки: lag_1, lag_7, lag_14, lag_30, lag_365
Почему нельзя использовать lag_0 (текущее значение)?

**2.3.** Создай скользящие статистики (всегда со сдвигом shift(1)!):
- rolling_mean_7, rolling_mean_30
- rolling_std_7, rolling_std_30
- rolling_min_7, rolling_max_7

**2.4.** Почему все скользящие признаки нужно создавать с shift(1)? Что произойдёт без сдвига?

---

### Часть 3 — Моделирование

**3.1.** Раздели данные: train = до 2023-10-01, test = с 2023-10-01 (последние 90 дней). Почему нельзя использовать случайный split?

**3.2.** Реализуй Walk-Forward Validation (5 splits) на train части. Метрика — RMSE и MAPE.

**3.3.** Обучи три модели и сравни на Walk-Forward CV:
- LightGBM
- LinearRegression (как baseline)
- ExtraTreesRegressor

**3.4.** Лучшую модель оцени на test. Построй график: реальные vs предсказанные значения.

---

### Часть 4 — Продвинутые методы (бонус ⭐⭐)

**4.1.** Попробуй Prophet для прогноза на 30 дней. Сравни с LightGBM по RMSE.

**4.2.** Добавь в модель is_holiday и promo как регрессоры. Улучшилось ли качество?

**4.3.** Реализуй рекурсивный прогноз на 30 дней вперёд с LightGBM:
- Предскажи день t+1
- Добавь предсказание как лаг для следующего шага
- Повтори 30 раз
Как накапливается ошибка с горизонтом прогноза?

---

## 📝 Требования к ответу

- Правильное разбиение (хронологическое, без перемешивания)
- Walk-Forward CV вместо обычного KFold
- График реальных vs предсказанных значений с доверительным интервалом
- RMSE и MAPE для каждой модели
- Вывод: какая модель лучше и почему
