# Урок 02. Матрицы и линейные преобразования

> **Зачем это нужно:** один слой нейросети — это умножение матрицы на вектор плюс смещение. Если вы понимаете, что такое матричное умножение геометрически, нейросети перестают быть магией.

---

## 1. Матрица — это таблица чисел

Матрица размера m × n — это таблица из m строк и n столбцов.

```python
import numpy as np
A = np.array([[1, 2, 3],
              [4, 5, 6]])      # shape (2, 3): 2 строки, 3 столбца
print(A.shape)   # (2, 3)
```

В ML почти всё — матрицы. Датасет: строки — объекты, столбцы — признаки. Веса слоя: строки — выходные нейроны, столбцы — входные.

---

## 2. Транспонирование

Транспонирование меняет строки и столбцы местами: `A.T[i, j] = A[j, i]`.

```python
A = np.array([[1, 2, 3], [4, 5, 6]])
print(A.T)
# [[1 4]
#  [2 5]
#  [3 6]]
print(A.T.shape)   # (3, 2)
```

Свойства: `(A.T).T = A`, `(A + B).T = A.T + B.T`, `(A @ B).T = B.T @ A.T` (внимание на порядок!).

---

## 3. Умножение матрицы на вектор

Это **главная операция в нейросетях**.

$$ y = A x, \quad y_i = \sum_{j} A_{ij} x_j $$

Геометрически: `A @ x` — это применение линейного преобразования `A` к точке `x`.

```python
A = np.array([[2, 0],
              [0, 3]])
x = np.array([1, 1])
print(A @ x)   # [2 3] — растяжение в 2 раза по X, в 3 раза по Y
```

Поворот на угол θ:

```python
theta = np.pi / 4
R = np.array([[np.cos(theta), -np.sin(theta)],
              [np.sin(theta),  np.cos(theta)]])
print(R @ np.array([1, 0]))   # [0.707, 0.707] — повернули на 45°
```

---

## 4. Умножение матриц

$$ (AB)_{ij} = \sum_{k} A_{ik} B_{kj} $$

Размеры: `(m × n) @ (n × p) = (m × p)`. Внутренние размеры должны совпадать.

```python
A = np.random.randn(3, 4)
B = np.random.randn(4, 5)
C = A @ B
print(C.shape)   # (3, 5)
```

**Ключевая интуиция:** `AB` — это композиция преобразований. Сначала применили `B`, потом `A`. Поэтому `AB ≠ BA` в общем случае.

---

## 5. Единичная и обратная матрицы

**Единичная матрица** `I` — единицы на диагонали, нули вне. Для любой матрицы `A·I = I·A = A`.

```python
I = np.eye(3)
print(I)
```

**Обратная** `A^{-1}` — такая, что `A · A^{-1} = I`. Существует только для квадратных невырожденных матриц.

```python
A = np.array([[1, 2], [3, 4]])
A_inv = np.linalg.inv(A)
print(A @ A_inv)   # почти единичная
```

В реальном коде **никогда не считайте `inv(A)` для решения систем** — используйте `np.linalg.solve(A, b)`. Это быстрее и численно устойчивее.

---

## 6. Линейная регрессия за 3 строки

Замечательное применение матриц. Решение методом наименьших квадратов:

$$ w = (X^T X)^{-1} X^T y $$

```python
X = np.random.randn(100, 5)        # 100 объектов, 5 признаков
true_w = np.array([1, -2, 3, 0.5, -1])
y = X @ true_w + 0.1 * np.random.randn(100)

w_hat = np.linalg.solve(X.T @ X, X.T @ y)
print(w_hat)   # должно быть близко к true_w
```

Это уже работающая линейная регрессия. Без `sklearn`. На чистом NumPy. Запомните этот момент.

---

## 7. Ранг и вырожденность

**Ранг матрицы** — число линейно независимых строк (или столбцов). Если ранг меньше размера — матрица вырождена, обратной нет.

```python
A = np.array([[1, 2], [2, 4]])     # вторая строка = 2 × первая
print(np.linalg.matrix_rank(A))    # 1
```

**Где это в ML:** если в датасете два признака линейно зависят друг от друга (например, температура в Цельсиях и в Фаренгейтах), матрица `X^T X` становится вырожденной и линейная регрессия ломается. Это решается регуляризацией (Ridge добавляет `λI`).

---

## 8. Практические задания

1. **Своё matmul.** Реализуйте умножение матриц без `@` и `np.dot`, через три вложенных цикла. Сравните скорость с NumPy на матрицах 200×200.

2. **Векторизация.** Перепишите своё matmul через NumPy broadcasting без явных циклов. Сколько раз быстрее?

3. **Поворот картинки.** Загрузите изображение, представьте каждый пиксель как точку (x, y), примените матрицу поворота на 30°. Сохраните результат.

4. **Линейная регрессия с нуля.** На датасете Boston Housing решите `X^T X w = X^T y`. Сравните MSE с `sklearn.LinearRegression`.

5. **Псевдообратная.** Реализуйте `pinv(A)` через SVD (потом поймёте, как). Проверьте, что `A @ pinv(A) @ A ≈ A`.

6. **Композиция преобразований.** Сделайте поворот на 30°, потом масштабирование в 2 раза по X. Найдите итоговую матрицу одним умножением. Проверьте, что результат совпадает.

7. **Проверка вырожденности.** Сгенерируйте 10 случайных матриц 5×5, добавьте к двум из них линейно зависимую строку. Найдите эти две через `matrix_rank`.

8. **Свой solve.** Реализуйте решение `Ax = b` методом Гаусса (прямой и обратный ход). Сравните с `np.linalg.solve`.

---

## ✅ Чек

- [ ] Умножаете матрицы руками на бумаге для 2×2.
- [ ] Можете объяснить, почему `AB ≠ BA`.
- [ ] Понимаете, что слой нейросети `Wx + b` — это аффинное преобразование.
- [ ] Решаете линейную регрессию через нормальные уравнения.
- [ ] Знаете, почему `solve` лучше `inv`.

[← Урок 01](./01-vectors.md) · [К курсу](./README.md) · [Урок 03: Собственные числа и SVD →](./03-eigen-svd.md)
