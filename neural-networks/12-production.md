# Урок 12. От модели к продакшну: ONNX, квантизация, инференс

> Цель урока: научиться доставлять обученную модель пользователю. Покрыть путь от `model.pt` до отвечающего API с предсказуемой задержкой и потреблением памяти.

## Что значит «положить модель в прод»

Это **минимум 5 проблем**, не одна:

1. **Сериализация.** Сохранить веса так, чтобы их можно было загрузить без исходного кода тренировки.
2. **Скорость инференса.** `model(x)` в обучении и в проде — это разные требования.
3. **Память.** 7B-модель в fp32 — это 28 GB. На прод-сервере столько может не быть.
4. **API.** Кто-то должен вызвать модель по HTTP и получить результат.
5. **Мониторинг.** Понимать, когда модель «поплыла» и пора переобучать.

## Шаг 1. Сохранение и загрузка

```python
# Веса — лёгкий, безопасный способ
torch.save(model.state_dict(), 'model.pt')

# Загрузка
model = MyModel()
model.load_state_dict(torch.load('model.pt', map_location='cpu'))
model.eval()
```

**Не используйте `torch.save(model)`** (без state_dict). Он сохраняет Python-объект, ломается при изменении кода и небезопасен при загрузке чужих файлов (могут содержать произвольный код).

## Шаг 2. Переход на ONNX

**ONNX (Open Neural Network Exchange)** — формат, который понимают почти все runtime'ы: ONNX Runtime, TensorRT, OpenVINO, CoreML. Один `.onnx` файл — и модель крутится на CPU, GPU, мобильном устройстве, в браузере.

```python
dummy = torch.randn(1, 3, 224, 224)
torch.onnx.export(
    model, dummy, "model.onnx",
    input_names=["input"], output_names=["output"],
    dynamic_axes={"input": {0: "batch"}, "output": {0: "batch"}},
    opset_version=17,
)

# Инференс через ONNX Runtime
import onnxruntime as ort
sess = ort.InferenceSession("model.onnx", providers=["CPUExecutionProvider"])
out = sess.run(None, {"input": dummy.numpy()})
```

На многих моделях ONNX Runtime даёт 2-5× ускорение на CPU по сравнению с обычным PyTorch.

## Шаг 3. Квантизация

Идея — хранить веса не в `float32`, а в `int8` или `int4`. Размер модели падает в 4-8 раз, скорость на CPU/edge — растёт.

**Post-training dynamic quantization** (простейший вариант):

```python
import torch.quantization as tq
quantized = tq.quantize_dynamic(model, {nn.Linear, nn.LSTM}, dtype=torch.qint8)
```

**Для LLM** используют более продвинутые методы: `bitsandbytes` (8 и 4 бита), `GPTQ` (4 бита, GPU), `AWQ` (Activation-aware), `GGUF` (формат llama.cpp для CPU).

Типичная просадка качества при int8 квантизации — 0-1%. При int4 — 1-3%. Это часто **приемлемая цена** за 4× ускорение и снижение памяти.

## Шаг 4. Простой API на FastAPI

```python
from fastapi import FastAPI
from pydantic import BaseModel
import onnxruntime as ort, numpy as np

app = FastAPI()
sess = ort.InferenceSession("model.onnx", providers=["CPUExecutionProvider"])

class Input(BaseModel):
    features: list[float]

@app.post("/predict")
def predict(inp: Input):
    x = np.array([inp.features], dtype=np.float32)
    y = sess.run(None, {"input": x})[0]
    return {"prediction": y[0].tolist()}
```

Запуск: `uvicorn app:app --host 0.0.0.0 --port 8000`. Контейнеризация — Dockerfile из 5 строк. Готово.

## Шаг 5. Batch inference и динамический батчинг

В проде запросы приходят по одному, но GPU работает эффективно только на батчах. Решение — **сервер с динамическим батчингом** (NVIDIA Triton, vLLM, TGI). Сервер сам собирает запросы, накопившиеся за 10-50 мс, в один батч.

Без батчинга 1 запрос = 100% занятость GPU на коротком отрезке. С батчингом — 16 запросов одновременно за то же время.

## Шаг 6. KV-кеш для LLM

При автогрессивной генерации каждый новый токен требует пересчёта attention по всему контексту. **KV-кеш** хранит уже посчитанные `K` и `V` от предыдущих токенов — пересчитывать нужно только последний.

Без кеша генерация N токенов = O(N²) операций. С кешем — O(N). На длинных контекстах разница в 100× и больше. В `transformers` это `use_cache=True` (по умолчанию включено).

## Шаг 7. Inference-движки для LLM

Не пишите свой LLM-сервер. Используйте готовое:

- **vLLM** — высокая пропускная способность, PagedAttention, continuous batching.
- **TGI (Text Generation Inference)** — HuggingFace, продакшн-ready.
- **llama.cpp / ollama** — для CPU и edge, формат GGUF.
- **Triton Inference Server** — универсальный, для любых моделей.
- **TensorRT-LLM** — для NVIDIA GPU, максимальная скорость.

Выбор: для CPU/локально — llama.cpp; для одного сервера с GPU — vLLM; для большой нагрузки в облаке — Triton или TGI.

## Шаг 8. Мониторинг и data drift

Модель в проде живёт **не вечно**. Распределение входных данных меняется, и метрики деградируют. Что мониторить:

- **Latency** (p50, p95, p99) и количество ошибок 5xx.
- **Распределение входных фичей** (Population Stability Index, KL-дивергенция от baseline).
- **Распределение предсказаний** (если модель начала всегда отвечать одинаково — это тревога).
- **Реальные метрики бизнеса** (CTR, конверсия, NPS) — главный сигнал.

Готовые инструменты: `Evidently AI`, `whylogs`, `Arize`.

## Чек-лист релиза модели

- [ ] Веса сохранены через `state_dict`, версия закоммичена.
- [ ] Есть скрипт инференса, который запускается без зависимостей от тренировочного кода.
- [ ] Модель сконвертирована в ONNX (или другой формат runtime'а).
- [ ] Замерены latency и throughput на целевом железе.
- [ ] Есть API с health-check и rate limiting.
- [ ] Логи запросов и ответов пишутся (с учётом PII).
- [ ] Настроен мониторинг распределений и метрик.
- [ ] Есть план переобучения и автоматический retraining pipeline.

## 8 практических заданий

1. **ONNX-экспорт.** Экспортируйте обученную ResNet18 (CIFAR-10) в ONNX. Сравните inference time PyTorch и ONNX Runtime на CPU.
2. **Квантизация CNN.** Примените `quantize_dynamic` к ResNet18. Замерьте: размер на диске, accuracy, скорость. Сделайте таблицу.
3. **FastAPI-сервис.** Оберните модель в FastAPI. Сделайте health-check, валидацию входа, корректные коды ошибок.
4. **Docker.** Напишите Dockerfile из <10 строк, который собирается в образ <500 MB и запускает ваш сервис.
5. **Нагрузочный тест.** Запустите `locust` или `wrk` против вашего API. Постройте график RPS vs latency.
6. **LLM на ноутбуке.** Скачайте Mistral 7B в GGUF Q4, запустите через `llama-cpp-python`. Замерьте tokens/s.
7. **vLLM сервер.** Поднимите Mistral 7B на одной GPU через vLLM. Сравните throughput с `transformers` на 50 параллельных запросах.
8. **Простой drift-monitor.** Реализуйте функцию, которая считает PSI между двумя выборками входных признаков. Запустите на сэмплах train и «свежих» данных вашей задачи.

## Чек-лист урока

- [ ] Я знаю, чем `state_dict` лучше полного pickle.
- [ ] Я экспортировал модель в ONNX и запустил её через ONNX Runtime.
- [ ] Я понимаю, что такое квантизация и какие у неё бывают типы.
- [ ] Я поднял реальный inference-API.
- [ ] Я понимаю, почему vLLM/TGI быстрее наивного `transformers.generate`.
- [ ] Я знаю, что такое data drift и как его мониторить.
- [ ] Я сделал хотя бы 5 заданий из 8.

---

## Поздравляю — курс пройден 🎉

Если вы дошли до сюда и сделали хотя бы половину практических заданий — вы:

- Понимаете, что такое нейросеть на уровне отдельных матричных операций.
- Можете собрать любую современную архитектуру **из примитивов**.
- Умеете обучать и дообучать модели на своих данных.
- Знаете, что делать с моделью **после** `accuracy = 0.94`.

Дальше — собирайте проекты, читайте свежие статьи на arXiv, участвуйте в Kaggle-соревнованиях. И не забывайте, что курс по математике [math-for-ml/](../math-for-ml/README.md) всегда рядом, если упрётесь в формулу.

---

◀︎ [Урок 11](./11-finetuning.md) · [README курса](./README.md) · 🏠 [Главный README](../README.md)
